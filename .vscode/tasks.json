{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Serve Nomadifly",
      "type": "shell",
      "command": "npx ng serve --open --host 0.0.0.0 --port 4200",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "isBackground": true,
      "problemMatcher": []
    },
    {
      "label": "Build Nomadifly",
      "type": "shell",
      "command": "npx ng build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
